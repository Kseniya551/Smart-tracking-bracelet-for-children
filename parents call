//(בשביל שהילד יקרא להורים)
<?php    //הגדרת שפת התכנות שבה אנחנו משתמשים פה
$val = $_GET["mun"];   //קולטים מספר מהצמיד ומכניסים אותו למשתנה
if($val == 1){   //תנאי שמתקיים אם המשתנה שווה ל1
       file_put_contents("try5.txt", $val);   //מכניסים את המידע שנמצא במשתנה לקובץ אחר 
}

if($val == 1){  //תנאי שמתקיים אם המשתנה שווה ל1
    sleep(50);   //השהייה למשך 50 שניות
    $val=0;   //מאפסים את המשתנה
    file_put_contents("try5.txt",  $val );   //מכניסים את המידע שנמצא במשתנה לקובץ אחר 
}
?>   //סוגרים את הגדרת שפת התכנות שבחרנו


